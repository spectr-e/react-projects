import { Button } from '../../components'

const SearchFilter = () => {
  return (
    <section className='text-center'>
      <Button classes={'btn-dark'} />
    </section>
  )
}

export default SearchFilter
