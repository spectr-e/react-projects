import { Title } from '../../components'

const FilterContactApp = () => {
  return (
    <div className='container'>
      <Title title={'Contacts'} classes={'title text-center'} />
    </div>
  )
}

export default FilterContactApp
