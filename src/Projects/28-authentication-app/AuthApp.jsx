import { Title } from '../../components'

const AuthApp = () => {
  return (
    <div className='container'>
      <Title title={'Authentication App'} classes={'title text-center'} />
    </div>
  )
}

export default AuthApp
